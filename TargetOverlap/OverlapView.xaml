<Window x:Class="TargetOverlap.OverlapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TargetOverlap"
             WindowStartupLocation="CenterOwner"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="350"
             Height="500" Width="350"
             Title="Structure Overlap Calculator">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="2">
            <Label Content="Select the structure to compute overlaps with:" FontSize="14" FontWeight="Bold"
                   HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
            <TextBlock Text="The Overlap Percentage shows the percent of the selected structure 
                       that overlaps with the structure in the table." 
                       FontSize="11" TextAlignment="Center" TextWrapping="Wrap" Margin="0,10,0,0"/>
        </StackPanel>
        
        <ComboBox Grid.Row="1" x:Name="Structures_comboBox" Height="25" Margin="10" HorizontalAlignment="Center" HorizontalContentAlignment="Left"/>
        <Button Grid.Row="2" Content="Calculate" Name="Calculate_Button"
                Width="80" Height="30" Margin="5" Click="Calculate_Button_Click" HorizontalAlignment="Center"/> 
        

        <DataGrid Grid.Row="3" x:Name="Result_DataGrid" ItemsSource="{Binding Overlaps}" Margin="10" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Structure" Binding="{Binding Structure}" Width="*" />
                <DataGridTextColumn Header="OverlapPercentage" Binding="{Binding OverlapPercentage}" Width="120" />
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Window>
